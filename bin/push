#!/usr/bin/env bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

cd $DIR/..
source .env

docker push ${PROJECT_NAMESPACE}/${REPOSITORY_NAME}-nginx:latest
docker push ${PROJECT_NAMESPACE}/${REPOSITORY_NAME}-php:latest
